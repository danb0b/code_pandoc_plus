\providecommand\myfontsize{11pt}
$if(font_size)$
\renewcommand\myfontsize{$font_size$}
$endif$
$if(fontsize)$
\renewcommand\myfontsize{$fontsize$}
$endif$

\documentclass[\myfontsize]{extarticle}

$if(overlay_text)$
\providecommand\MyOverlayText{$overlay_text$}
$endif$

\setcounter{secnumdepth}{0}
$if(numbersections)$
\setcounter{secnumdepth}{$numbersections$}
$endif$
$if(number_sections)$
\setcounter{secnumdepth}{$number_sections$}
$endif$

$if(spacing)$
\providecommand\myspacing{$spacing$}
$endif$

$if(parskip)$
\providecommand\myparskip{$parskip$}
$endif$

$if(headerskip)$
\providecommand\myheaderskip{$headerskip$}
$endif$

$if(parindent)$
\providecommand\myparindent{$parindent$}
$endif$

%$if(border)$
%\providecommand\myborder{$border$}
%$endif$

$if(figure_alignment)$
\providecommand\myfigurealignment{$figure_alignment$}
$endif$

\usepackage{pandoc}

$if(title)$
\providecommand\mytitle{$title$}
$endif$

$if(subtitle)$
\providecommand\mysubtitle{$subtitle$}
$endif$

\providecommand\myclasslogo{egr557_logo}
%\providecommand\myclassname{EGR557}

$if(university_logo)$
\providecommand\myuniversitylogo{$university_logo$}
$endif$

\providecommand\mycopyright{Daniel M. Aukes}

$if(pagenumber)$
\providecommand\mypagenumber{$pagenumber$}
$endif$

\usepackage{class}

\begin{document}

$body$

\end{document}
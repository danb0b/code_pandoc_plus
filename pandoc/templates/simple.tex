\providecommand\myfontsize{10pt}
$if(font_size)$
\renewcommand\myfontsize{$font_size$}
$endif$
$if(fontsize)$
\renewcommand\myfontsize{$fontsize$}
$endif$

\documentclass[\myfontsize]{extarticle}

$if(overlay_text)$
\providecommand\MyOverlayText{$overlay_text$}
$endif$

\setcounter{secnumdepth}{0}
$if(numbersections)$
\setcounter{secnumdepth}{$numbersections$}
$endif$
$if(number_sections)$
\setcounter{secnumdepth}{$number_sections$}
$endif$

$if(mainfont)$
\providecommand\myfont{$mainfont$}
$endif$

$if(sansfont)$
\providecommand\mysansfont{$sansfont$}
$endif$

$if(spacing)$
\providecommand\myspacing{$spacing$}
$endif$

$if(parskip)$
\providecommand\myparskip{$parskip$}
$endif$

$if(headerskip)$
\providecommand\myheaderskip{$headerskip$}
$endif$

$if(parindent)$
\providecommand\myparindent{$parindent$}
$endif$

$if(border)$
\providecommand\myborder{$border$}
$endif$

$if(figure_alignment)$
\providecommand\myfigurealignment{$figure_alignment$}
$endif$

$if(copyright)$
\providecommand\mycopyright{$copyright$}
$endif$

$if(footer_text)$
\providecommand\myfootertext{$footer_text$}
$endif$

$if(header_text)$
\providecommand\myheadertext{$header_text$}
$endif$

$if(pagenumber)$
\providecommand\mypagenumber{$pagenumber$}
$endif$

\usepackage{pandoc}
\usepackage{simple}


\begin{document}

$if(title)$
\MakeMyTitle{$title$}
$endif$

$body$

\end{document}
